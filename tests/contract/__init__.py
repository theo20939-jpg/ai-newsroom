"""Provider-adapter contract tests (docs/phase7_architecture_contract.md §20.1 names this
directory/file explicitly as the provider-onboarding runbook's step 2 gate). Every adapter -
fake or real - must pass the tests here before being registered anywhere."""
