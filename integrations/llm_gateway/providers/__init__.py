"""Provider adapters and the provider-registration machinery (Phase 7).

A provider adapter is exactly an LLMGateway implementation scoped to one
provider's SDK (docs/phase7_architecture_contract.md §2, P12) - no separate
"provider adapter" Protocol exists.
"""
